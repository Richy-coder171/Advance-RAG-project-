# HR Policy Documents

`official/` contains only the exact 11 Zyro Dynamics competition PDFs.

The pipeline validates every filename and SHA-256 hash before loading documents.
Adding, removing, or modifying a file causes startup and submission generation to fail.
